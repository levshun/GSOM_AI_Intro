---
description: Explains a command or flag for a beginner. Usage: /explain ls -la
agent: shell-tutor
---

Explain the command below for a beginner using the Shell Tutor agent: its exact
meaning, the parts (name, options, arguments), one safe example using the local
`sample/` folder, and a safety note. Keep it under 15 lines.

$ARGUMENTS

If words like "destroy", "delete" or "remove" appear in the command, lead with a
safety warning and suggest a non-destructive alternative.