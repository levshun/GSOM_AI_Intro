---
description: Writes a safe Bash script for a task. Usage: /script 'back up sample/logs into sandbox/backups with a timestamp'
agent: script-writer
---

Write a safe, defensive Bash script for the task below using the Script Writer
agent. Save it to `scripts/` and report the path, how to run it safely, and what
it does. No `rm`, `sudo`, or destructive commands; quote every variable.

$ARGUMENTS